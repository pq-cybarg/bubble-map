# Speculative overlay — super-connectors, the IC/VC pipeline, and the control-layer hypothesis

*Built from `research/spec-network-overlay.json`. Extends `spec-crypto-sec-epstein`; actors cross-referenced in `temporal-bridges` / `data/temporal_web.json`.*

> **WARNING — read first.** This is an **evidence-graded speculative overlay, kept OUT of the formal proofs.** Each thread is graded **fact | contested | weak | unsupported.** The connective tissue is **overlapping actors / capital / institutions — NOT demonstrated coordination.** Documented **association ≠ coordination ≠ wrongdoing** by the associated. **Intent is never inferred from adjacency.** Nothing here is represented as proven.

## What this layer is for
The formal core (the SCC/Z3/TLA+ proofs on `data/graph.json`) deliberately excludes relationship/association edges. This overlay collects the **graded "who-overlaps-whom"** observations — the structural questions worth holding in view — **without** letting them contaminate the proofs. It is a *map of proximity*, explicitly not a map of conspiracy.

## Representative threads (each graded; full set in the JSON)
- **Ellison super-connector** — *individual links STRONG; unified "control agenda" SPECULATIVE.* Larry Ellison sits at an unusual intersection: **Oracle** (AI circular core) + **Stargate** (AI SPV) + the **Tony Blair Institute** (~$348M → UK digital-ID policy, `influence-tbi-policy`) + **Oracle running the TikTok-US algorithm** (China-app data) + dominant **US/UK government cloud.** *What it shows:* one person is **structurally positioned to benefit** from the digital-ID buildout his foundation funds. *What it does NOT show:* any coordinated plan — **convergent self-interest + philanthropy-as-influence explains it without intent.**
- **IC/VC pipeline** — documented personnel/capital flows between intelligence backgrounds and venture/defense-tech (In-Q-Tel lineage, Palantir/Anduril networks) — *association documented; "coordination" unsupported.*
- **The control-layer hypothesis** — the broad question of whether compute + identity + payments + surveillance are converging into a single control stack. Treated as a **hypothesis to test, graded down to its documented components**, never asserted as a unified plan.

## The discipline this layer enforces
Every entry pairs **"what it shows"** with **"what it does NOT show."** That pairing is the point: the project's credibility depends on separating **documented structural proximity** (real, useful) from **inferred coordination** (unsupported, excluded). This is where the sensitive, suggestive material lives **so it stays out of the proofs** — visible, graded, and quarantined.

*Sources: per-thread URLs in the JSON; sensitive personal threads use documented public reporting only. See `influence-tbi-policy`, `influence-operator-network`, `spec-crypto-sec-epstein`, `temporal-bridges`.*
